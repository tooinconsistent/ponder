{
  "name": "db",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "generate:queries": "pgtyped -c pgtyped-config.json && bun rewrite-pg-import.ts && cd ../api && bun run format"
  },
  "devDependencies": {
    "@pgtyped/cli": "^2.0.0",
    "typescript": "^4.9.5",
    "bun-types": "^0.5.0"
  },
  "dependencies": {
    "replace-in-file": "^6.3.5"
  }
}
